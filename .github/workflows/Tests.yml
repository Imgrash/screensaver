---
name: Tests

on: push

jobs:
  tests:
    name: Unit Testing
    runs-on: windows-latest
    steps:
      - name: check repo
        uses: actions/checkout@v2.1.0
      - name: tests
        run: dotnet test
